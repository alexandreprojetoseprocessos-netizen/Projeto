const NotFoundPage = () => (
  <div className="page-card">
    <h1>Página não encontrada</h1>
    <p className="muted">Verifique o endereço ou use o menu lateral para navegar.</p>
  </div>
);

export default NotFoundPage;
